# project overview

- The sql folder contains SQL scripts for setting up the database schema and seeding initial data.
- The database is a postgreSQL instance running in a Docker container, as defined in the docker-compose.yml file.
- The .env file contains environment variables for configuring the database connection and credentials.

